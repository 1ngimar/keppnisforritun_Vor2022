aDouble = (ld)a;
            // tempDouble2 = tempDouble / aDouble;
            // temp2 = temp / a;
            // if (tempDouble2 != temp2)
            // {
            //     continue;
            // }